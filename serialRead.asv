function out = serialRead(s,id)

switch id
    case strcmp(id,'booth1')
        escapeCode = '1!';
    case strcmp(id,'booth2')
        escapeCode = '2@';
    case strcmp(id,'booth3')
        escapeCode = 

while 1
    if s.BytesAvailable
        out = fscanf(s);
        break
    end
    [~,~,keyCode] = KbCheck;
    if sum(keyCode) >= 2
        if strcmp(KbName(keyCode),'ESCAPE');
            out = '0000 999999 USEREXIT';
            break
        end
    end
end
